# Database
DATABASE_URL="postgresql://dealflow:dealflow_local_dev@localhost:5432/dealflow?schema=public"

# Redis (not currently used — safe to leave blank)
# REDIS_URL="redis://localhost:6379"

# Encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY="your-32-byte-hex-key-here"

# Microsoft 365 / Outlook Integration
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID=""
# For production: update to your Railway/production URL
AZURE_AD_REDIRECT_URI="http://localhost:3000/api/email/auth/callback"

# Apify (BizBuySell scraper - $45/mo actor rental + platform credits)
# Get your token at: https://console.apify.com/account/integrations
APIFY_API_TOKEN=""

# Google / Gmail Integration
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# For production: update to your Railway/production URL
GOOGLE_REDIRECT_URI="http://localhost:3000/api/email/auth/callback/gmail"

# Google Maps (for Colorado Front Range map view)
# Get your key at: https://console.cloud.google.com/apis/credentials
# Create a Map ID at: https://console.cloud.google.com/google/maps-platform/map-management
NEXT_PUBLIC_GOOGLE_MAPS_KEY=""
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=""

# App URL (MUST be updated for production deployment)
# Local: http://localhost:3000
# Railway: https://your-app.up.railway.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# API Authentication (optional — leave blank to skip auth in dev)
# Set a strong random string in production
# API_KEY=""

# Anthropic Claude API (for AI features: scoring, news classification, valuation commentary)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""

# Anthropic Admin API (optional — for real-time cost/usage monitoring in Agent Dashboard)
# Requires an organization account. Generate at: https://console.anthropic.com/settings/admin-keys
# Without this, the dashboard shows estimated costs from local agent run data.
ANTHROPIC_ADMIN_API_KEY=""

# Cron Secret (protects background agent endpoints from unauthorized access)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Set this in Railway env vars AND in your external cron service (cron-job.org)
CRON_SECRET=""
