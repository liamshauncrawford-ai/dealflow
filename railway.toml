[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node node_modules/prisma/build/index.js migrate deploy && node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
